import { css } from "@emotion/css";
import { forsize } from "@mixins";

export const section = forsize({
  "desktop-mid": css``,
});

export const h1 = forsize({
  "desktop-mid": css``,
});

export const welcome = forsize({
  "desktop-mid": css``,
});

export const image = forsize({
  "desktop-mid": css``,
});
