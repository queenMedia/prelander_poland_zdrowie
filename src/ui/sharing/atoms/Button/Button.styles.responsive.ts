import { css } from "@emotion/css";
import { forsize } from "@mixins";

export const button = forsize({
  "desktop-mid": css``,
});

export const gradient = forsize({
  "desktop-mid": css``,
});

export const outline = forsize({
  "desktop-mid": css``,
});

export const invert = forsize({
  "desktop-mid": css``,
});

export const size_normal = forsize({
  "desktop-mid": css``,
});

export const size_big = forsize({
  "desktop-mid": css``,
});

export const size_small = forsize({
  "desktop-mid": css``,
});

export const text_normal = forsize({
  "desktop-mid": css``,
});

export const text_upper = forsize({
  "desktop-mid": css``,
});

export const text_lower = forsize({
  "desktop-mid": css``,
});

export const text_capital = forsize({
  "desktop-mid": css``,
});
