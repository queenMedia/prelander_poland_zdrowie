import { css, cx} from "@emotion/css";

import * as responsive from "./Customtext.styles.responsive";
import { flex } from "@mixins";

export const title = cx(
  css`
    font-size: 40px;
  `
);

export const subtitle = cx(
  css`
    font-size: 20px;
  `
);

export const paragraph = cx(
  css`
    font-size: 15px;
  `
);